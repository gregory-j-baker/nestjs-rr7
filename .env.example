# Example environment variables for the nestjs-rr7 server
# Copy this file to `.env` and adjust values for your environment.

# Runtime mode used to choose production vs development behavior.
# Allowed values: development, production, test
# Default: development
NODE_ENV=development

# HTTP port used by the Nest server `listen` call.
# Must be an integer between 1 and 65535.
# Default: 3000
PORT=3000

# Whether debug features are enabled. Accepts true/false (case-insensitive).
# Default: false
DEBUG=false

# Whether telemetry/metrics collection is enabled. Accepts true/false.
# When true, OpenTelemetry SDK will be initialized and started.
# Default: false
TELEMETRY_ENABLED=false

# OpenTelemetry OTLP HTTP exporter endpoint (including protocol).
# Exporter URLs are used as the base; traces are sent to `${endpoint}/v1/traces`
# and metrics to `${endpoint}/v1/metrics`.
# Default: http://localhost:4318
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318

# Logical service name for telemetry (OTEL_SERVICE_NAME).
# Default: nestjs-rr7
OTEL_SERVICE_NAME=nestjs-rr7

# Service version for telemetry (OTEL_SERVICE_VERSION).
# Default: 0.0.0
OTEL_SERVICE_VERSION=0.0.0

# Export interval for telemetry metrics in milliseconds.
# Must be a positive integer. Default: 60000 (60 seconds)
OTEL_EXPORT_INTERVAL_MS=60000

# Upstream GitHub status summary endpoint consumed by server-side services.
# Must be a valid absolute URL. Default: https://www.githubstatus.com/api/v2/summary.json
GITHUB_STATUS_URL=https://www.githubstatus.com/api/v2/summary.json

# Cache TTL (milliseconds) for GitHub status responses. Must be >= 0.
# Default: 60000 (60 seconds)
GITHUB_STATUS_CACHE_TTL_MS=60000

# Which GitHub status provider to use at runtime.
# Allowed values: mock, default
# Default: mock
GITHUB_STATUS_PROVIDER_TYPE=mock

# Timeout in milliseconds for GitHub status requests.
# Must be a positive integer. Default: 2000 (2 seconds)
GITHUB_STATUS_TIMEOUT_MS=2000
